//- Single Page
mixin page(item)
    .post.page
        article.hentry(itemscope itemtype='http://schema.org/Article')
            .entry-header
                h1.entry-title
                    != item.title
                .entry-description
                    != item.excerpt
            .entry-content(itemprop='articleBody')
                != item.more
